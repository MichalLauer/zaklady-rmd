---
title: "Hlavní nadpis"
subtitle: "Tohle je podnadpis"
author:
  - John Doe^[Institution One, john@example.org]
  - Jane Doe^[Institution Two, jane@example.org]
date: "`r format(Sys.time(), '%d %B, %Y')`"
output:
  bookdown::html_document2:
    lof: yes
bibliography: citace.bib
cls: iso690.cls
lof: yes
knit: (function(inputFile, encoding) {
          rmarkdown::render(inputFile,
                            encoding = encoding,
                            output_dir = "output/")})
params:
  region: 
---

Hodnota parametru: `r params$region`


Viz. \@ref(eq:def)

$$
x = 3 (\#eq:def)
$$


# Knihovny

```{r }
library(dplyr)
library(ggplot2)
library(gt)
```

# Načtení dat {#data-load}

Bez závorek: @HebakEtAl2015_StatistickeMysleniNastroje

Se závorkami: [@HebakEtAl2015_StatistickeMysleniNastroje]

Upřesnění: [@HebakEtAl2015_StatistickeMysleniNastroje, s. 80]

Víc citací: [@HebakEtAl2015_StatistickeMysleniNastroje; @MocEtAl2013_MatematikaProEkonomy,]

Načtení dat o autech, jsme v [této](#data-load) kapitole.

Tabulka \@ref(tab:mtc)

```{r mtc, echo=FALSE}
gt(head(mtcars)) |> 
  tab_caption(
    caption = "A table of mtcars"
  )
```

Tabulka \@ref(tab:mtc)

## Vizualizace

Jednoduchý graf, viz \@ref(fig:muj-grafik).

```{r muj-grafik, fig.cap="Popis obrázku"}
g1 <-
  ggplot(mtcars, aes(x = factor(cyl), y = mpg)) +
  geom_boxplot() +
  theme_classic()

plotly::ggplotly(g1)
```

Průměr je popsán v rovnici \@ref(eq:mean)

```{=tex}
\begin{equation}
\bar{X} = \frac{\sum_{i=1}^n X_i}{n} (\#eq:mean)
\end{equation}
```

# a

## b

### c

## d

## Reference

```{r }

```




```{r }
# Create a gt table using the `exibble` dataset
exibble |>
  select(-c(fctr, date, time, datetime)) |>
  gt(rowname_col = "row", groupname_col = "group") |>
  sub_missing() |>
  summary_rows(
    groups = everything(),
    columns = num,
    fns = list(
      avg = ~ mean(.x, na.rm = TRUE)
    )
  )
  
```
